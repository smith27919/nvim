# Neovim state that is machine-local, not configuration.
# lazy-lock.json is deliberately NOT ignored: it pins plugin versions, which is
# what makes a cloned config reproduce this exact setup.
spell/
shada/
*.shada
.netrwhist

# Machine-local: colors, overrides, and the lua/<username> link.
colors/
lua/*
!lua/core/
